# Full developer environment for zapme.
#
# Includes everything: Pi-runtime deps + training stack + dev tools.
# The source of truth for the *runtime* subset is pyproject.toml's
# `[project.dependencies]`; this file mirrors it plus the `train` and
# `dev` extras for one-shot `pip install -r requirements.txt`.
#
# Pi deployment should NOT use this file — use `pip install -e .[pi]`
# instead, which skips the training/dev stack entirely.

# --- Pi-runtime core (mirrors pyproject [project.dependencies]) ---
huggingface_hub
numpy
onnxruntime
opencv-python
ultralytics
pandas

# --- Training stack (mirrors pyproject [train] extra) ---
torch
torchvision
transformers
datasets
bitsandbytes
onnx
pyarrow

# --- Dev tools (mirrors pyproject [dev] extra) ---
pytest
ruff

# --- Pi-only hardware I/O. Resolves only on Linux aarch64. ---
lgpio;     sys_platform == "linux" and platform_machine == "aarch64"
gpiozero;  sys_platform == "linux" and platform_machine == "aarch64"
