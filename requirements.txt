# Top-level dependencies for zapme.
# Source of truth lives in pyproject.toml; this file mirrors it for
# convenience (`pip install -r requirements.txt`).
#
# Pi-only deps use environment markers, so this file installs cleanly
# on both Windows (skips Pi deps) and the Pi (installs everything).

# Core ML / HF stack
huggingface_hub[cli]
transformers
datasets
bitsandbytes
torch
torchvision
pillow
numpy
onnx
onnxruntime
opencv-python
ultralytics
pyarrow
pandas

# Utils
pytest

# Pi-only hardware I/O. Resolves only on Linux aarch64 (Pi OS 64-bit).
lgpio;     sys_platform == "linux" and platform_machine == "aarch64"
gpiozero;  sys_platform == "linux" and platform_machine == "aarch64"
